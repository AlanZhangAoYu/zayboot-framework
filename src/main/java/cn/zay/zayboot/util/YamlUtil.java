package cn.zay.zayboot.util;

import java.util.Map;

/**
 * @author ZAY
 * 解析 yaml 文件的工具类
 */
public class YamlUtil {
    /**
     * 根据传入的路径输出值 例如从 server.port 获取到 8080
     * @param path 传入的路径
     * @return 获取到的值(默认为String类型，实际用时再转换)
     */
    public static String getInfoFromFile(String path){

    }
}
