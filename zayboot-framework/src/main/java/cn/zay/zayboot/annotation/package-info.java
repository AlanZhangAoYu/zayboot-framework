/**
 * 该框架要实现的注解统一放在此包下管理
 * <p>目录结构及解释: </p>
 * <p>aop - 切面(Aspect)、切点(Pointcut)、要切入的方法以及运行方式(After、Before)</p>
 * <p>boot - 自动装配、包扫描等</p>
 * <p>config - 注入从配置文件读入的值</p>
 * <p>ioc - 生成 bean对象, 依赖注入等</p>
 * <p>springmvc - URL与 Controller映射、接收参数处理等</p>
 * <p>validation - 数据校验</p>
 */
package cn.zay.zayboot.annotation;
